{
  "name": "efes-musag-bakaduregel",
  "private": true,
  "version": "1.0.0",
  "description": "0 מושג בכדורגל — ליגת ניחושי תוצאות פרטית לליגת העל הישראלית",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "seed": "npm run seed -w server",
    "seed:demo": "npm run seed:demo -w server",
    "promote-admin": "npm run promote-admin -w server",
    "reset-password": "npm run reset-password -w server",
    "test": "npm run test -w shared && npm run test -w server",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w server && npm run typecheck -w client",
    "e2e": "npm run e2e -w client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
